{
    "sourceFile": "2601_notification_manager.py",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 112,
            "patches": [
                {
                    "date": 1768696621212,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1768696721989,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -148,13 +148,11 @@\n                 logger.warning(f\"Invalid email format: {person}\")\n                 continue\n             if person.count('@'):\n                 #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.cards.create(\n+                api.messages.create(\n                     toPersonEmail=person,\n-                    markdown=\"**This is a card**\",\n-                    title=\"Notification Card\",\n-                    text=\"This card contains important information.\",\n+                    attachments=[card_content]\n                 )\n             else:\n                 api.messages.create(\n                     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n"
                },
                {
                    "date": 1768696862929,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -150,8 +150,9 @@\n             if person.count('@'):\n                 #api.messages.create(toPersonEmail=person, text=contextStr)\n                 api.messages.create(\n                     toPersonEmail=person,\n+                    text=\"Notification card (client fallback)\",\n                     attachments=[card_content]\n                 )\n             else:\n                 api.messages.create(\n"
                },
                {
                    "date": 1768696888137,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,171 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_webex(self, subject, context, to_list):\n+        # 初始化 API\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        summary = \" \".join(context).split('|')\n+        contextStr = subject + \"\\n\" + \"\\n\".join(summary)\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.2\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": \"Notification Card\",\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": \"This card contains important information.\",\n+                        \"wrap\": True\n+                    }\n+                ]\n+            }\n+        }\n+\n+        # 发送消息\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        context=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768696894444,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -168,175 +168,4 @@\n         subject=\"Regression Notification\",\n         context=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_webex(self, subject, context, to_list):\n-        # 初始化 API\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        summary = \" \".join(context).split('|')\n-        contextStr = subject + \"\\n\" + \"\\n\".join(summary)\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.2\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": \"Notification Card\",\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": \"This card contains important information.\",\n-                        \"wrap\": True\n-                    }\n-                ]\n-            }\n-        }\n-\n-        # 发送消息\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=\"Notification card (client fallback)\",\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768697901103,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -124,9 +124,9 @@\n                 \"version\": \"1.2\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": \"Notification Card\",\n+                        \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     {\n"
                },
                {
                    "date": 1768698004350,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -112,10 +112,10 @@\n         # 初始化 API\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        summary = \" \".join(context).split('|')\n-        contextStr = subject + \"\\n\" + \"\\n\".join(summary)\n+        msg = \" \".join(context).split('|')\n+        contextStr = subject + \"\\n\" + \"\\n\".join(msg)\n \n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n"
                },
                {
                    "date": 1768698049857,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -113,9 +113,9 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \" \".join(context).split('|')\n-        contextStr = subject + \"\\n\" + \"\\n\".join(msg)\n+        contextStr = \"\\n\".join(msg)\n \n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n"
                },
                {
                    "date": 1768698106454,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -112,10 +112,10 @@\n         # 初始化 API\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        msg = \" \".join(context).split('|')\n-        contextStr = \"\\n\".join(msg)\n+        msg = \n+        contextStr = \"\\n\".join(\" \".join(context).split('|'))\n \n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n"
                },
                {
                    "date": 1768698118190,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -112,10 +112,9 @@\n         # 初始化 API\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        msg = \n-        contextStr = \"\\n\".join(\" \".join(context).split('|'))\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n \n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n"
                },
                {
                    "date": 1768698126895,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,170 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_webex(self, subject, context, to_list):\n+        # 初始化 API\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.2\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": msg,\n+                        \"wrap\": True\n+                    }\n+                ]\n+            }\n+        }\n+\n+        # 发送消息\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        context=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768698132436,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -167,174 +167,4 @@\n         subject=\"Regression Notification\",\n         context=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_webex(self, subject, context, to_list):\n-        # 初始化 API\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.2\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": \"This card contains important information.\",\n-                        \"wrap\": True\n-                    }\n-                ]\n-            }\n-        }\n-\n-        # 发送消息\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768698155067,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -119,9 +119,9 @@\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n                 \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n                 \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.2\",\n+                \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n                         \"text\": subject,\n@@ -136,9 +136,9 @@\n                 ]\n             }\n         }\n \n-        # 发送消息\n+        # send\n         for person in to_list:\n             if not person:\n                 continue\n             logger.info(f\"Sending Webex message to {person}\")\n"
                },
                {
                    "date": 1768698161652,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -136,9 +136,9 @@\n                 ]\n             }\n         }\n \n-        # send\n+        # send message\n         for person in to_list:\n             if not person:\n                 continue\n             logger.info(f\"Sending Webex message to {person}\")\n"
                },
                {
                    "date": 1768698166950,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -108,9 +108,8 @@\n         finally:\n             server.quit()\n \n     def notify_user_by_webex(self, subject, context, to_list):\n-        # 初始化 API\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n"
                },
                {
                    "date": 1768699013811,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,8 +107,49 @@\n             logger.warning(f\"fail to send mail: {e}\")\n         finally:\n             server.quit()\n \n+def build_summary_columnset(summary_text):\n+    \"\"\"\n+    Convert 'key : value' lines into an Adaptive Card ColumnSet\n+    compatible with Cisco Webex (Adaptive Card v1.2).\n+    \"\"\"\n+    left_items = []\n+    right_items = []\n+\n+    for line in summary_text.strip().splitlines():\n+        if \":\" not in line:\n+            continue\n+\n+        key, value = map(str.strip, line.split(\":\", 1))\n+\n+        left_items.append({\n+            \"type\": \"TextBlock\",\n+            \"text\": key,\n+            \"wrap\": True\n+        })\n+\n+        right_items.append({\n+            \"type\": \"TextBlock\",\n+            \"text\": value,\n+            \"wrap\": True\n+        })\n+\n+    return {\n+        \"type\": \"ColumnSet\",\n+        \"columns\": [\n+            {\n+                \"type\": \"Column\",\n+                \"width\": \"stretch\",\n+                \"items\": left_items\n+            },\n+            {\n+                \"type\": \"Column\",\n+                \"width\": \"auto\",\n+                \"items\": right_items\n+            }\n+        ]\n+    }\n     def notify_user_by_webex(self, subject, context, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n"
                },
                {
                    "date": 1768699024353,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,49 +107,50 @@\n             logger.warning(f\"fail to send mail: {e}\")\n         finally:\n             server.quit()\n \n-def build_summary_columnset(summary_text):\n-    \"\"\"\n-    Convert 'key : value' lines into an Adaptive Card ColumnSet\n-    compatible with Cisco Webex (Adaptive Card v1.2).\n-    \"\"\"\n-    left_items = []\n-    right_items = []\n+    def build_summary_columnset(summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n \n-    for line in summary_text.strip().splitlines():\n-        if \":\" not in line:\n-            continue\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n \n-        key, value = map(str.strip, line.split(\":\", 1))\n+            key, value = map(str.strip, line.split(\":\", 1))\n \n-        left_items.append({\n-            \"type\": \"TextBlock\",\n-            \"text\": key,\n-            \"wrap\": True\n-        })\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n \n-        right_items.append({\n-            \"type\": \"TextBlock\",\n-            \"text\": value,\n-            \"wrap\": True\n-        })\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n \n-    return {\n-        \"type\": \"ColumnSet\",\n-        \"columns\": [\n-            {\n-                \"type\": \"Column\",\n-                \"width\": \"stretch\",\n-                \"items\": left_items\n-            },\n-            {\n-                \"type\": \"Column\",\n-                \"width\": \"auto\",\n-                \"items\": right_items\n-            }\n-        ]\n-    }\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n     def notify_user_by_webex(self, subject, context, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n"
                },
                {
                    "date": 1768699084117,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -168,13 +168,9 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": msg,\n-                        \"wrap\": True\n-                    }\n+                    build_summary_columnset(summary_text)\n                 ]\n             }\n         }\n \n"
                },
                {
                    "date": 1768699089708,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -168,9 +168,9 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    build_summary_columnset(summary_text)\n+                    build_summary_columnset(msg)\n                 ]\n             }\n         }\n \n"
                },
                {
                    "date": 1768699098548,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -168,9 +168,9 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    build_summary_columnset(msg)\n+                    self.build_summary_columnset(msg)\n                 ]\n             }\n         }\n \n"
                },
                {
                    "date": 1768699209423,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -168,9 +168,9 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    self.build_summary_columnset(msg)\n+                    build_summary_columnset(msg)\n                 ]\n             }\n         }\n \n"
                },
                {
                    "date": 1768699217519,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,207 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, context, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        context=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768699223305,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,9 +107,9 @@\n             logger.warning(f\"fail to send mail: {e}\")\n         finally:\n             server.quit()\n \n-    def build_summary_columnset(summary_text):\n+    def build_summary_columnset(self, summary_text):\n         \"\"\"\n         Convert 'key : value' lines into an Adaptive Card ColumnSet\n         compatible with Cisco Webex (Adaptive Card v1.2).\n         \"\"\"\n@@ -204,211 +204,4 @@\n         subject=\"Regression Notification\",\n         context=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, context, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768699327999,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -139,13 +139,15 @@\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n                     \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\"\n                     \"items\": left_items\n                 },\n                 {\n                     \"type\": \"Column\",\n                     \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\"\n                     \"items\": right_items\n                 }\n             ]\n         }\n"
                },
                {
                    "date": 1768699334095,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -139,15 +139,15 @@\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n                     \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\"\n+                    \"horizontalAlignment\": \"Left\",\n                     \"items\": left_items\n                 },\n                 {\n                     \"type\": \"Column\",\n                     \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\"\n+                    \"horizontalAlignment\": \"Left\",\n                     \"items\": right_items\n                 }\n             ]\n         }\n"
                },
                {
                    "date": 1768699410873,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -138,9 +138,9 @@\n             \"type\": \"ColumnSet\",\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n+                    \"width\": \"auto\",\n                     \"horizontalAlignment\": \"Left\",\n                     \"items\": left_items\n                 },\n                 {\n"
                },
                {
                    "date": 1768699431267,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,210 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, context, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        context=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768700102449,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -138,9 +138,8 @@\n             \"type\": \"ColumnSet\",\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n-                    \n                     \"width\": \"auto\",\n                     \"horizontalAlignment\": \"Left\",\n                     \"items\": left_items\n                 },\n@@ -171,217 +170,16 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, context, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n                     {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n+    \"type\": \"TextBlock\",\n+    \"text\": \"Test Execution Summary\",\n+    \"weight\": \"Bolder\",\n+    \"size\": \"Medium\",\n+    \"horizontalAlignment\": \"Left\",\n+    \"spacing\": \"Medium\"\n+}\n                     self.build_summary_columnset(msg)\n                 ]\n             }\n         }\n"
                },
                {
                    "date": 1768700111210,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,217 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, context, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": \"Test Execution Summary\",\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\",\n+                        \"horizontalAlignment\": \"Left\",\n+                        \"spacing\": \"Medium\"\n+                    },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        context=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768700130993,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -214,221 +214,4 @@\n         subject=\"Regression Notification\",\n         context=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, context, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    {\n-    \"type\": \"TextBlock\",\n-    \"text\": \"Test Execution Summary\",\n-    \"weight\": \"Bolder\",\n-    \"size\": \"Medium\",\n-    \"horizontalAlignment\": \"Left\",\n-    \"spacing\": \"Medium\"\n-}\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768700595139,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -156,9 +156,11 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n-\n+        if msg.count('Test results summary:'):\n+            logger.info(\"No tests were found, skip sending Webex message\")\n+            return\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n                 \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n"
                },
                {
                    "date": 1768700619079,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -157,10 +157,9 @@\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n-            logger.info(\"No tests were found, skip sending Webex message\")\n-            return\n+            subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n                 \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n"
                },
                {
                    "date": 1768700688182,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -121,8 +121,11 @@\n                 continue\n \n             key, value = map(str.strip, line.split(\":\", 1))\n \n+            if \":\" not in line:\n+                continue\n+\n             left_items.append({\n                 \"type\": \"TextBlock\",\n                 \"text\": key,\n                 \"wrap\": True\n"
                },
                {
                    "date": 1768700709676,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -121,9 +121,9 @@\n                 continue\n \n             key, value = map(str.strip, line.split(\":\", 1))\n \n-            if \":\" not in line:\n+            if value == \"\" or key == \"\":\n                 continue\n \n             left_items.append({\n                 \"type\": \"TextBlock\",\n"
                },
                {
                    "date": 1768700714811,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -118,9 +118,8 @@\n \n         for line in summary_text.strip().splitlines():\n             if \":\" not in line:\n                 continue\n-\n             key, value = map(str.strip, line.split(\":\", 1))\n \n             if value == \"\" or key == \"\":\n                 continue\n"
                },
                {
                    "date": 1768700720086,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -118,8 +118,9 @@\n \n         for line in summary_text.strip().splitlines():\n             if \":\" not in line:\n                 continue\n+\n             key, value = map(str.strip, line.split(\":\", 1))\n \n             if value == \"\" or key == \"\":\n                 continue\n"
                },
                {
                    "date": 1768700783273,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -174,16 +174,16 @@\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": \"Test Execution Summary\",\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\",\n-                        \"horizontalAlignment\": \"Left\",\n-                        \"spacing\": \"Medium\"\n-                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n                     self.build_summary_columnset(msg)\n                 ]\n             }\n         }\n"
                },
                {
                    "date": 1768701608261,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -170,9 +170,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": subject,\n+                        \"text\": f\"[subject]({subject})\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768701744868,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -170,9 +170,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": f\"[subject]({subject})\",\n+                        \"text\": f\"{subject}[subject](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768701858691,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -159,8 +159,10 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n+        db_build_id = \"\"\n+        msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n"
                },
                {
                    "date": 1768701877547,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,9 +158,9 @@\n     def notify_user_by_webex(self, subject, context, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n+        msg = \" \".join(context).split('|')\n         db_build_id = \"\"\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n"
                },
                {
                    "date": 1768701902209,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -154,9 +154,9 @@\n                 }\n             ]\n         }\n \n-    def notify_user_by_webex(self, subject, context, to_list):\n+    def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         msg = \" \".join(context).split('|')\n"
                },
                {
                    "date": 1768701907747,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,9 +158,9 @@\n     def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        msg = \" \".join(context).split('|')\n+        msg = \" \".join(content).split('|')\n         db_build_id = \"\"\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n"
                },
                {
                    "date": 1768701913068,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -217,7 +217,7 @@\n     # Example usage\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n         subject=\"Regression Notification\",\n-        context=sys.argv[2:],\n+        content=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n"
                },
                {
                    "date": 1768702029371,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,9 +158,9 @@\n     def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        msg = \" \".join(content).split('|')\n+        content_lst = \" \".join(content).split('|')\n         db_build_id = \"\"\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n"
                },
                {
                    "date": 1768702035641,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -159,9 +159,9 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         content_lst = \" \".join(content).split('|')\n-        db_build_id = \"\"\n+        db_build_id = content_lst[0].strip()\n         msg = \"\\n\".join(\" \".join(context).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n"
                },
                {
                    "date": 1768702064380,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -160,9 +160,9 @@\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         content_lst = \" \".join(content).split('|')\n         db_build_id = content_lst[0].strip()\n-        msg = \"\\n\".join(\" \".join(context).split('|'))\n+        msg = \"\\n\".join(\" \".join(content_lst[1:]).split('|'))\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n"
                },
                {
                    "date": 1768702203884,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -172,9 +172,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject}[subject](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"text\": f\"{subject}[{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768702335759,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -172,9 +172,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject}[{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768702381222,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,223 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        content_lst = \" \".join(content).split('|')\n+        db_build_id = content_lst[0].strip()\n+        msg = \"\\n\".join(content_lst[1:])\n+        if msg.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:\n+                api.messages.create(\n+                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768702576164,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -220,227 +220,4 @@\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        content_lst = \" \".join(content).split('|')\n-        db_build_id = content_lst[0].strip()\n-        msg = \"\\n\".join(\" \".join(content_lst[1:]).split('|'))\n-        if msg.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:\n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n-\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768702624641,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -176,16 +176,8 @@\n                         \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n                     self.build_summary_columnset(msg)\n                 ]\n             }\n         }\n"
                },
                {
                    "date": 1768702645011,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -176,8 +176,16 @@\n                         \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n                     self.build_summary_columnset(msg)\n                 ]\n             }\n         }\n"
                },
                {
                    "date": 1768702704337,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -205,9 +205,9 @@\n                     toPersonEmail=person,\n                     text=subject,\n                     attachments=[card_content]\n                 )\n-            else:\n+            else:                \n                 api.messages.create(\n                     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n                     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n                 )\n"
                },
                {
                    "date": 1768702709386,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -206,12 +206,12 @@\n                     text=subject,\n                     attachments=[card_content]\n                 )\n             else:                \n-                api.messages.create(\n-                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                )\n+                # api.messages.create(\n+                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                # )\n \n \n if __name__ == \"__main__\":\n     # Example usage\n"
                },
                {
                    "date": 1768702715649,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -210,8 +210,13 @@\n                 # api.messages.create(\n                 #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n                 #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n                 # )\n+                api.messages.create(\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n \n \n if __name__ == \"__main__\":\n     # Example usage\n"
                },
                {
                    "date": 1768703138278,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,9 +158,12 @@\n     def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n+ \n         content_lst = \" \".join(content).split('|')\n+        if content.count(\"db_build_id\"):\n+            is None or len(content) == 0:\n         db_build_id = content_lst[0].strip()\n         msg = \"\\n\".join(content_lst[1:])\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n@@ -216,9 +219,8 @@\n                     text=subject,\n                     attachments=[card_content]\n                 )\n \n-\n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n"
                },
                {
                    "date": 1768703159176,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,229 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+ \n+        content_lst = \" \".join(content).split('|')\n+        if content.count(\"db_build_id\"):\n+            db_build_id = content_lst[0].strip()\n+        msg = \"\\n\".join(content_lst[1:])\n+        if msg.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:                \n+                # api.messages.create(\n+                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                # )\n+                api.messages.create(\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768703167992,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,229 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, to_list):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+ \n+        content_lst = \" \".join(content).split('|')\n+        if content.count(\"db_build_id\"):\n+            db_build_id = content_lst[0].strip()\n+            msg = \"\\n\".join(content_lst[1:])\n+        if msg.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        for person in to_list:\n+            if not person:\n+                continue\n+            logger.info(f\"Sending Webex message to {person}\")\n+            # check if person is a valid email format\n+            if not isinstance(person, str):\n+                logger.warning(f\"Invalid email format: {person}\")\n+                continue\n+            if person.count('@'):\n+                #api.messages.create(toPersonEmail=person, text=contextStr)\n+                api.messages.create(\n+                    toPersonEmail=person,\n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+            else:                \n+                # api.messages.create(\n+                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+                # )\n+                api.messages.create(\n+                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                    text=subject,\n+                    attachments=[card_content]\n+                )\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        to_list=[sys.argv[1]],\n+    )\n"
                },
                {
                    "date": 1768703193335,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -163,8 +163,10 @@\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n             msg = \"\\n\".join(content_lst[1:])\n+        else:\n+            \n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n@@ -226,463 +228,4 @@\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n         to_list=[sys.argv[1]],\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n- \n-        content_lst = \" \".join(content).split('|')\n-        if content.count(\"db_build_id\"):\n-            db_build_id = content_lst[0].strip()\n-        msg = \"\\n\".join(content_lst[1:])\n-        if msg.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:                \n-                # api.messages.create(\n-                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                # )\n-                api.messages.create(\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, to_list):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n- \n-        content_lst = \" \".join(content).split('|')\n-        if content.count(\"db_build_id\"):\n-            is None or len(content) == 0:\n-        db_build_id = content_lst[0].strip()\n-        msg = \"\\n\".join(content_lst[1:])\n-        if msg.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@'):\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:                \n-                # api.messages.create(\n-                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                # )\n-                api.messages.create(\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n-    )\n"
                },
                {
                    "date": 1768703201095,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -164,9 +164,9 @@\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n-            \n+            msg = \"\\n\".join(content_lst[1:])\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n"
                },
                {
                    "date": 1768703209252,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -164,9 +164,9 @@\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n-            msg = \"\\n\".join(content_lst[1:])\n+            msg = \"\\n\".join(content_lst)\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n"
                },
                {
                    "date": 1768703239081,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,17 +158,17 @@\n     def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n- \n+        if msg.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n-        if msg.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n+\n         card_content = {\n             \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n             \"content\": {\n                 \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n"
                },
                {
                    "date": 1768703263033,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -163,8 +163,9 @@\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n+            f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n \n@@ -176,9 +177,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\",\n+                        \"text\": ,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768703271480,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -163,9 +163,9 @@\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].strip()\n-            f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n+            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n \n"
                },
                {
                    "date": 1768703287694,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -177,9 +177,9 @@\n                 \"version\": \"1.3\",\n                 \"body\": [\n                     {\n                         \"type\": \"TextBlock\",\n-                        \"text\": ,\n+                        \"text\": subject,\n                         \"weight\": \"Bolder\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n"
                },
                {
                    "date": 1768703325335,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -162,9 +162,9 @@\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n-            db_build_id = content_lst[0].strip()\n+            db_build_id = content_lst[0].split('|')[0].strip()\n             subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n"
                },
                {
                    "date": 1768703341582,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -162,9 +162,9 @@\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split('|')[0].strip()\n+            db_build_id = content_lst[0].split('|')[1].strip()\n             subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n"
                },
                {
                    "date": 1768703350529,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -162,9 +162,9 @@\n         if msg.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split('|')[1].strip()\n+            db_build_id = content_lst[0].split(':')[1].strip()\n             subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n             msg = \"\\n\".join(content_lst)\n"
                },
                {
                    "date": 1768703391978,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -158,9 +158,9 @@\n     def notify_user_by_webex(self, subject, content, to_list):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n-        if msg.count('Test results summary:'):\n+        if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content.count(\"db_build_id\"):\n             db_build_id = content_lst[0].split(':')[1].strip()\n"
                },
                {
                    "date": 1768704389315,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -161,9 +161,9 @@\n         )\n         if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n-        if content.count(\"db_build_id\"):\n+        if content_lst[0].count(\"db_build_id\"):\n             db_build_id = content_lst[0].split(':')[1].strip()\n             subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n             msg = \"\\n\".join(content_lst[1:])\n         else:\n"
                },
                {
                    "date": 1768705132489,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -203,9 +203,9 @@\n             # check if person is a valid email format\n             if not isinstance(person, str):\n                 logger.warning(f\"Invalid email format: {person}\")\n                 continue\n-            if person.count('@'):\n+            if person.count('@')==1:\n                 #api.messages.create(toPersonEmail=person, text=contextStr)\n                 api.messages.create(\n                     toPersonEmail=person,\n                     text=subject,\n"
                },
                {
                    "date": 1768719955400,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -227,6 +227,6 @@\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n-        to_list=[sys.argv[1]],\n+        to_list=sys.argv[1],\n     )\n"
                },
                {
                    "date": 1768719970555,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -227,6 +227,6 @@\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n-        to_list=sys.argv[1],\n+        to_list=sys.argv[1].strip(),\n     )\n"
                },
                {
                    "date": 1768719976438,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -227,6 +227,6 @@\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n-        to_list=sys.argv[1].strip(),\n+        to_list=sys.argv[1].strip()\n     )\n"
                },
                {
                    "date": 1768719986975,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -227,6 +227,6 @@\n     nm = NotificationManager()\n     nm.notify_user_by_webex(\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n-        to_list=sys.argv[1].strip()\n+        webex_id=sys.argv[1].strip()\n     )\n"
                },
                {
                    "date": 1768720000245,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -154,9 +154,9 @@\n                 }\n             ]\n         }\n \n-    def notify_user_by_webex(self, subject, content, to_list):\n+    def notify_user_by_webex(self, subject, content, webex_id):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n         if content.count('Test results summary:'):\n"
                },
                {
                    "date": 1768720026113,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -195,33 +195,32 @@\n             }\n         }\n \n         # send message\n-        for person in to_list:\n-            if not person:\n-                continue\n-            logger.info(f\"Sending Webex message to {person}\")\n-            # check if person is a valid email format\n-            if not isinstance(person, str):\n-                logger.warning(f\"Invalid email format: {person}\")\n-                continue\n-            if person.count('@')==1:\n-                #api.messages.create(toPersonEmail=person, text=contextStr)\n-                api.messages.create(\n-                    toPersonEmail=person,\n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n-            else:                \n-                # api.messages.create(\n-                #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-                #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-                # )\n-                api.messages.create(\n-                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                    text=subject,\n-                    attachments=[card_content]\n-                )\n+        if not person:\n+            continue\n+        logger.info(f\"Sending Webex message to {person}\")\n+        # check if person is a valid email format\n+        if not isinstance(person, str):\n+            logger.warning(f\"Invalid email format: {person}\")\n+            continue\n+        if person.count('@')==1:\n+            #api.messages.create(toPersonEmail=person, text=contextStr)\n+            api.messages.create(\n+                toPersonEmail=person,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        else:                \n+            # api.messages.create(\n+            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+            # )\n+            api.messages.create(\n+                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                text=subject,\n+                attachments=[card_content]\n+            )\n \n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n"
                },
                {
                    "date": 1768720056133,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -195,10 +195,10 @@\n             }\n         }\n \n         # send message\n-        if not person:\n-            continue\n+        if not webex_id:\n+            return\n         logger.info(f\"Sending Webex message to {person}\")\n         # check if person is a valid email format\n         if not isinstance(person, str):\n             logger.warning(f\"Invalid email format: {person}\")\n"
                },
                {
                    "date": 1768720061667,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -196,8 +196,9 @@\n         }\n \n         # send message\n         if not webex_id:\n+            logger.warning(\"No Webex ID provided for notification.\")\n             return\n         logger.info(f\"Sending Webex message to {person}\")\n         # check if person is a valid email format\n         if not isinstance(person, str):\n"
                },
                {
                    "date": 1768720073703,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -198,12 +198,12 @@\n         # send message\n         if not webex_id:\n             logger.warning(\"No Webex ID provided for notification.\")\n             return\n-        logger.info(f\"Sending Webex message to {person}\")\n-        # check if person is a valid email format\n-        if not isinstance(person, str):\n-            logger.warning(f\"Invalid email format: {person}\")\n+        logger.info(f\"Sending Webex message to {webex_id}\")\n+        # check if webex_id is a valid email format\n+        if not isinstance(webex_id, str):\n+            logger.warning(f\"Invalid email format: {webex_id}\")\n             continue\n         if person.count('@')==1:\n             #api.messages.create(toPersonEmail=person, text=contextStr)\n             api.messages.create(\n"
                },
                {
                    "date": 1768720087596,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -202,9 +202,9 @@\n         logger.info(f\"Sending Webex message to {webex_id}\")\n         # check if webex_id is a valid email format\n         if not isinstance(webex_id, str):\n             logger.warning(f\"Invalid email format: {webex_id}\")\n-            continue\n+            return\n         if person.count('@')==1:\n             #api.messages.create(toPersonEmail=person, text=contextStr)\n             api.messages.create(\n                 toPersonEmail=person,\n"
                },
                {
                    "date": 1768720092920,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,232 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, webex_id):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        if content.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        content_lst = \" \".join(content).split('|')\n+        if content_lst[0].count(\"db_build_id\"):\n+            db_build_id = content_lst[0].split(':')[1].strip()\n+            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n+            msg = \"\\n\".join(content_lst[1:])\n+        else:\n+            msg = \"\\n\".join(content_lst)\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        if not webex_id:\n+            logger.warning(\"No Webex ID provided for notification.\")\n+            return\n+        logger.info(f\"Sending Webex message to {webex_id}\")\n+        # check if webex_id is a valid email format\n+        if not isinstance(webex_id, str):\n+            logger.warning(f\"Invalid email format: {webex_id}\")\n+            return\n+        if webex_id.count('@')==1:\n+            #api.messages.create(toPersonEmail=person, text=contextStr)\n+            api.messages.create(\n+                toPersonEmail=person,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        else:                \n+            # api.messages.create(\n+            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+            # )\n+            api.messages.create(\n+                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                text=subject,\n+                attachments=[card_content]\n+            )\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        webex_id=sys.argv[1].strip()\n+    )\n"
                },
                {
                    "date": 1768720098085,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -206,9 +206,9 @@\n             return\n         if webex_id.count('@')==1:\n             #api.messages.create(toPersonEmail=person, text=contextStr)\n             api.messages.create(\n-                toPersonEmail=person,\n+                toPersonEmail=webex_id,\n                 text=subject,\n                 attachments=[card_content]\n             )\n         else:                \n@@ -229,236 +229,4 @@\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n         webex_id=sys.argv[1].strip()\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, webex_id):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        if content.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        content_lst = \" \".join(content).split('|')\n-        if content_lst[0].count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split(':')[1].strip()\n-            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n-            msg = \"\\n\".join(content_lst[1:])\n-        else:\n-            msg = \"\\n\".join(content_lst)\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        if not webex_id:\n-            logger.warning(\"No Webex ID provided for notification.\")\n-            return\n-        logger.info(f\"Sending Webex message to {webex_id}\")\n-        # check if webex_id is a valid email format\n-        if not isinstance(webex_id, str):\n-            logger.warning(f\"Invalid email format: {webex_id}\")\n-            return\n-        if person.count('@')==1:\n-            #api.messages.create(toPersonEmail=person, text=contextStr)\n-            api.messages.create(\n-                toPersonEmail=person,\n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        else:                \n-            # api.messages.create(\n-            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-            # )\n-            api.messages.create(\n-                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                text=subject,\n-                attachments=[card_content]\n-            )\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        webex_id=sys.argv[1].strip()\n-    )\n"
                },
                {
                    "date": 1768720107138,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -204,9 +204,9 @@\n         if not isinstance(webex_id, str):\n             logger.warning(f\"Invalid email format: {webex_id}\")\n             return\n         if webex_id.count('@')==1:\n-            #api.messages.create(toPersonEmail=person, text=contextStr)\n+            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n             api.messages.create(\n                 toPersonEmail=webex_id,\n                 text=subject,\n                 attachments=[card_content]\n"
                },
                {
                    "date": 1768720151221,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -210,9 +210,9 @@\n                 toPersonEmail=webex_id,\n                 text=subject,\n                 attachments=[card_content]\n             )\n-        else:                \n+        else:\n             # api.messages.create(\n             #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n             #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n             # )\n"
                },
                {
                    "date": 1768720207239,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -210,8 +210,17 @@\n                 toPersonEmail=webex_id,\n                 text=subject,\n                 attachments=[card_content]\n             )\n+        elif webex_id.count('-')==4:\n+            # api.messages.create(\n+            #     toPersonId=webex_id, text=contextStr\n+            # )\n+            api.messages.create(\n+                toPersonId=webex_id,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n         else:\n             # api.messages.create(\n             #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n             #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n"
                },
                {
                    "date": 1768720222227,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -212,25 +212,18 @@\n                 attachments=[card_content]\n             )\n         elif webex_id.count('-')==4:\n             # api.messages.create(\n-            #     toPersonId=webex_id, text=contextStr\n-            # )\n-            api.messages.create(\n-                toPersonId=webex_id,\n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        else:\n-            # api.messages.create(\n             #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n             #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n             # )\n             api.messages.create(\n                 roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n                 text=subject,\n                 attachments=[card_content]\n             )\n+        else:\n+            \n \n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n"
                },
                {
                    "date": 1768720229189,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -221,9 +221,10 @@\n                 text=subject,\n                 attachments=[card_content]\n             )\n         else:\n-            \n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+           \n \n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n"
                },
                {
                    "date": 1768720240191,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -222,9 +222,9 @@\n                 attachments=[card_content]\n             )\n         else:\n             logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-           \n+       \n \n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n"
                },
                {
                    "date": 1768720246085,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,234 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, webex_id):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        if content.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        content_lst = \" \".join(content).split('|')\n+        if content_lst[0].count(\"db_build_id\"):\n+            db_build_id = content_lst[0].split(':')[1].strip()\n+            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n+            msg = \"\\n\".join(content_lst[1:])\n+        else:\n+            msg = \"\\n\".join(content_lst)\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        if not webex_id:\n+            logger.warning(\"No Webex ID provided for notification.\")\n+            return\n+        logger.info(f\"Sending Webex message to {webex_id}\")\n+        # check if webex_id is a valid email format\n+        if not isinstance(webex_id, str):\n+            logger.warning(f\"Invalid email format: {webex_id}\")\n+            return\n+        if webex_id.count('@')==1:\n+            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n+            api.messages.create(\n+                toPersonEmail=webex_id,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        elif webex_id.count('-')==4:\n+            # api.messages.create(\n+            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+            # )\n+            api.messages.create(\n+                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        else:\n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        webex_id=sys.argv[1].strip()\n+    )\n"
                },
                {
                    "date": 1768720268840,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -201,244 +201,10 @@\n             return\n         logger.info(f\"Sending Webex message to {webex_id}\")\n         # check if webex_id is a valid email format\n         if not isinstance(webex_id, str):\n-            logger.warning(f\"Invalid email format: {webex_id}\")\n-            return\n-        if webex_id.count('@')==1:\n-            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n-            api.messages.create(\n-                toPersonEmail=webex_id,\n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        elif webex_id.count('-')==4:\n-            # api.messages.create(\n-            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-            # )\n-            api.messages.create(\n-                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        else:\n             logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        webex_id=sys.argv[1].strip()\n-    )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, webex_id):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        if content.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        content_lst = \" \".join(content).split('|')\n-        if content_lst[0].count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split(':')[1].strip()\n-            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n-            msg = \"\\n\".join(content_lst[1:])\n-        else:\n-            msg = \"\\n\".join(content_lst)\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        if not webex_id:\n-            logger.warning(\"No Webex ID provided for notification.\")\n             return\n-        logger.info(f\"Sending Webex message to {webex_id}\")\n-        # check if webex_id is a valid email format\n-        if not isinstance(webex_id, str):\n-            logger.warning(f\"Invalid email format: {webex_id}\")\n-            return\n         if webex_id.count('@')==1:\n             #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n             api.messages.create(\n                 toPersonEmail=webex_id,\n@@ -456,9 +222,8 @@\n                 attachments=[card_content]\n             )\n         else:\n             logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-       \n \n if __name__ == \"__main__\":\n     # Example usage\n     nm = NotificationManager()\n"
                },
                {
                    "date": 1768720281798,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,234 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"auto\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, webex_id):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+        if content.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        content_lst = \" \".join(content).split('|')\n+        if content_lst[0].count(\"db_build_id\"):\n+            db_build_id = content_lst[0].split(':')[1].strip()\n+            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n+            msg = \"\\n\".join(content_lst[1:])\n+        else:\n+            msg = \"\\n\".join(content_lst)\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        if not webex_id:\n+            logger.warning(\"No Webex ID provided for notification.\")\n+            return\n+        logger.info(f\"Sending Webex message to {webex_id}\")\n+        # check if webex_id is a valid email format\n+        if not isinstance(webex_id, str):\n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+            return\n+        if webex_id.count('@')==1:\n+            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n+            api.messages.create(\n+                toPersonEmail=webex_id,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        elif webex_id.count('-')==4:\n+            # api.messages.create(\n+            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+            # )\n+            api.messages.create(\n+                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        else:\n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        webex_id=sys.argv[1].strip()\n+    )\n"
                },
                {
                    "date": 1768720288889,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -231,238 +231,4 @@\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n         webex_id=sys.argv[1].strip()\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"auto\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, webex_id):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-        if content.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        content_lst = \" \".join(content).split('|')\n-        if content_lst[0].count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split(':')[1].strip()\n-            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n-            msg = \"\\n\".join(content_lst[1:])\n-        else:\n-            msg = \"\\n\".join(content_lst)\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        if not webex_id:\n-            logger.warning(\"No Webex ID provided for notification.\")\n-            return\n-        logger.info(f\"Sending Webex message to {webex_id}\")\n-        # check if webex_id is a valid email format\n-        if not isinstance(webex_id, str):\n-            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-            return\n-        if webex_id.count('@')==1:\n-            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n-            api.messages.create(\n-                toPersonEmail=webex_id,\n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        elif webex_id.count('-')==4:\n-            # api.messages.create(\n-            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-            # )\n-            api.messages.create(\n-                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        else:\n-            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        webex_id=sys.argv[1].strip()\n-    )\n"
                },
                {
                    "date": 1768720795018,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -179,8 +179,9 @@\n                     {\n                         \"type\": \"TextBlock\",\n                         \"text\": subject,\n                         \"weight\": \"Bolder\",\n+                        \"horizontalAlignment\": \"Center\",\n                         \"size\": \"Medium\"\n                     },\n                     # {\n                     #     \"type\": \"TextBlock\",\n"
                },
                {
                    "date": 1768720897413,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -182,8 +182,13 @@\n                         \"weight\": \"Bolder\",\n                         \"horizontalAlignment\": \"Center\",\n                         \"size\": \"Medium\"\n                     },\n+                    {\n+  \"type\": \"TextBlock\",\n+  \"text\": \"\",\n+  \"spacing\": \"Medium\"\n+}\n                     # {\n                     #     \"type\": \"TextBlock\",\n                     #     \"text\": \"Test Execution Summary\",\n                     #     \"weight\": \"Bolder\",\n"
                },
                {
                    "date": 1768720908601,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -183,12 +183,12 @@\n                         \"horizontalAlignment\": \"Center\",\n                         \"size\": \"Medium\"\n                     },\n                     {\n-  \"type\": \"TextBlock\",\n-  \"text\": \"\",\n-  \"spacing\": \"Medium\"\n-}\n+                        \"type\": \"TextBlock\",\n+                        \"text\": \"\",\n+                        \"spacing\": \"Medium\"\n+                    },\n                     # {\n                     #     \"type\": \"TextBlock\",\n                     #     \"text\": \"Test Execution Summary\",\n                     #     \"weight\": \"Bolder\",\n"
                },
                {
                    "date": 1768720972511,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -183,12 +183,18 @@\n                         \"horizontalAlignment\": \"Center\",\n                         \"size\": \"Medium\"\n                     },\n                     {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": \"\",\n-                        \"spacing\": \"Medium\"\n-                    },\n+  \"type\": \"ColumnSet\",\n+  \"separator\": true,\n+  \"columns\": [\n+    {\n+      \"type\": \"Column\",\n+      \"width\": \"stretch\",\n+      \"items\": []\n+    }\n+  ]\n+},\n                     # {\n                     #     \"type\": \"TextBlock\",\n                     #     \"text\": \"Test Execution Summary\",\n                     #     \"weight\": \"Bolder\",\n"
                },
                {
                    "date": 1768720981628,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -184,9 +184,9 @@\n                         \"size\": \"Medium\"\n                     },\n                     {\n   \"type\": \"ColumnSet\",\n-  \"separator\": true,\n+  \"separator\": True,\n   \"columns\": [\n     {\n       \"type\": \"Column\",\n       \"width\": \"stretch\",\n"
                },
                {
                    "date": 1768721056045,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -183,18 +183,18 @@\n                         \"horizontalAlignment\": \"Center\",\n                         \"size\": \"Medium\"\n                     },\n                     {\n-  \"type\": \"ColumnSet\",\n-  \"separator\": True,\n-  \"columns\": [\n-    {\n-      \"type\": \"Column\",\n-      \"width\": \"stretch\",\n-      \"items\": []\n-    }\n-  ]\n-},\n+                        \"type\": \"ColumnSet\",\n+                        \"separator\": True,\n+                        \"columns\": [\n+                            {\n+                            \"type\": \"Column\",\n+                            \"width\": \"stretch\",\n+                            \"items\": []\n+                            }\n+                        ]\n+                    },\n                     # {\n                     #     \"type\": \"TextBlock\",\n                     #     \"text\": \"Test Execution Summary\",\n                     #     \"weight\": \"Bolder\",\n"
                },
                {
                    "date": 1768721460064,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -141,9 +141,9 @@\n             \"type\": \"ColumnSet\",\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n-                    \"width\": \"auto\",\n+                    \"width\": \"stretch\",\n                     \"horizontalAlignment\": \"Left\",\n                     \"items\": left_items\n                 },\n                 {\n"
                },
                {
                    "date": 1768722318474,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -228,9 +228,9 @@\n             #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n             #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n             # )\n             api.messages.create(\n-                roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", \n+                roomId=webex_id, \n                 text=subject,\n                 attachments=[card_content]\n             )\n         else:\n"
                },
                {
                    "date": 1768737512005,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -124,8 +124,10 @@\n \n             if value == \"\" or key == \"\":\n                 continue\n \n+            if key.count(\"percentage\"):\n+                key\n             left_items.append({\n                 \"type\": \"TextBlock\",\n                 \"text\": key,\n                 \"wrap\": True\n"
                },
                {
                    "date": 1768737528076,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -125,9 +125,9 @@\n             if value == \"\" or key == \"\":\n                 continue\n \n             if key.count(\"percentage\"):\n-                key\n+                key = key.replace(\"percentage\", \"%\")\n             left_items.append({\n                 \"type\": \"TextBlock\",\n                 \"text\": key,\n                 \"wrap\": True\n"
                },
                {
                    "date": 1768737563413,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -123,11 +123,11 @@\n             key, value = map(str.strip, line.split(\":\", 1))\n \n             if value == \"\" or key == \"\":\n                 continue\n-\n             if key.count(\"percentage\"):\n                 key = key.replace(\"percentage\", \"%\")\n+\n             left_items.append({\n                 \"type\": \"TextBlock\",\n                 \"text\": key,\n                 \"wrap\": True\n"
                },
                {
                    "date": 1769231411541,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -160,8 +160,13 @@\n     def notify_user_by_webex(self, subject, content, webex_id):\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n+\n+        rooms = api.rooms.list()\n+        for room in rooms:\n+            print(room.title, room.id)\n+\n         if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n         if content_lst[0].count(\"db_build_id\"):\n"
                },
                {
                    "date": 1769231429015,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -161,11 +161,11 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n \n-        rooms = api.rooms.list()\n-        for room in rooms:\n-            print(room.title, room.id)\n+        # rooms = api.rooms.list()\n+        # for room in rooms:\n+        #     print(room.title, room.id)\n \n         if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n"
                },
                {
                    "date": 1769231455117,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -161,8 +161,9 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n \n+        # get room list for debug\n         # rooms = api.rooms.list()\n         # for room in rooms:\n         #     print(room.title, room.id)\n \n"
                },
                {
                    "date": 1769231460498,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,254 @@\n+\"\"\"\n+Notification manager for the Lab Management System.\n+\"\"\"\n+\n+import logging\n+from typing import Dict, Any\n+import smtplib\n+from webexteamssdk import WebexTeamsAPI\n+from email.mime.text import MIMEText\n+from email.mime.multipart import MIMEMultipart\n+import sys\n+\n+# Configure logging\n+logger = logging.getLogger(__name__)\n+\n+\n+class NotificationManager:\n+    \"\"\"\n+    Manages notifications for the Lab Management System.\n+    Handles Webex notifications.\n+    \"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize the notification manager.\"\"\"\n+        logger.info(\"Notification Manager initialized\")\n+\n+    def send_device_notification(\n+        self, device_id: int, message: str, data: Dict[str, Any]\n+    ):\n+        \"\"\"\n+        Send a notification about a device event.\n+\n+        Args:\n+            device_id: ID of the device\n+            message: Notification message\n+            data: Additional data for the notification\n+        \"\"\"\n+        # This is a placeholder implementation\n+        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n+\n+        # TODO: Implement actual notification sending via Webex\n+        # For now, just log the message\n+        return True\n+\n+    def notify_user_by_email(self, subject, context, email_list):\n+        # subject = f'Subject: {subject} \\n\\n {context}'\n+        # configure SMTP server\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        sender_email = \"no-reply@cisco.com\"\n+\n+        recipient_email = email_list\n+        message = MIMEMultipart()\n+        message[\"Subject\"] = subject\n+        message[\"From\"] = sender_email\n+        message[\"To\"] = \", \".join(email_list)\n+        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n+        try:\n+            with smtplib.SMTP(smtp_server, port) as server:\n+                server.starttls()\n+                server.sendmail(sender_email, recipient_email, message.as_string())\n+\n+                logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+\n+    def notify_user_by_email_with_plain_txt(\n+        self, subject: str, body: str, email_list: list\n+    ):\n+        msg = MIMEMultipart()\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n+        msg = MIMEMultipart(\"alternative\")\n+        msg[\"From\"] = \"no-reply@cisco.com\"\n+        msg[\"To\"] = \", \".join(email_list)\n+        msg[\"Subject\"] = subject\n+        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n+\n+        smtp_server = \"outbound.cisco.com\"\n+        port = 25\n+        recipient_email = email_list\n+        try:\n+            server = smtplib.SMTP(smtp_server, port)\n+            server.starttls()\n+            text = msg.as_string()\n+            server.sendmail(msg[\"From\"], recipient_email, text)\n+            logger.info(\"send mail successfully\")\n+        except Exception as e:\n+            logger.warning(f\"fail to send mail: {e}\")\n+        finally:\n+            server.quit()\n+\n+    def build_summary_columnset(self, summary_text):\n+        \"\"\"\n+        Convert 'key : value' lines into an Adaptive Card ColumnSet\n+        compatible with Cisco Webex (Adaptive Card v1.2).\n+        \"\"\"\n+        left_items = []\n+        right_items = []\n+\n+        for line in summary_text.strip().splitlines():\n+            if \":\" not in line:\n+                continue\n+\n+            key, value = map(str.strip, line.split(\":\", 1))\n+\n+            if value == \"\" or key == \"\":\n+                continue\n+            if key.count(\"percentage\"):\n+                key = key.replace(\"percentage\", \"%\")\n+\n+            left_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": key,\n+                \"wrap\": True\n+            })\n+\n+            right_items.append({\n+                \"type\": \"TextBlock\",\n+                \"text\": value,\n+                \"wrap\": True\n+            })\n+\n+        return {\n+            \"type\": \"ColumnSet\",\n+            \"columns\": [\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": left_items\n+                },\n+                {\n+                    \"type\": \"Column\",\n+                    \"width\": \"stretch\",\n+                    \"horizontalAlignment\": \"Left\",\n+                    \"items\": right_items\n+                }\n+            ]\n+        }\n+\n+    def notify_user_by_webex(self, subject, content, webex_id):\n+        api = WebexTeamsAPI(\n+            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n+        )\n+\n+        # get room list for debug\n+        # rooms = api.rooms.list()\n+        # for room in rooms:\n+        #     print(room.title, room.id)\n+\n+        if content.count('Test results summary:'):\n+            subject = \"Test Results Summary\"\n+        content_lst = \" \".join(content).split('|')\n+        if content_lst[0].count(\"db_build_id\"):\n+            db_build_id = content_lst[0].split(':')[1].strip()\n+            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n+            msg = \"\\n\".join(content_lst[1:])\n+        else:\n+            msg = \"\\n\".join(content_lst)\n+\n+        card_content = {\n+            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n+            \"content\": {\n+                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n+                \"type\": \"AdaptiveCard\",\n+                \"version\": \"1.3\",\n+                \"body\": [\n+                    {\n+                        \"type\": \"TextBlock\",\n+                        \"text\": subject,\n+                        \"weight\": \"Bolder\",\n+                        \"horizontalAlignment\": \"Center\",\n+                        \"size\": \"Medium\"\n+                    },\n+                    {\n+                        \"type\": \"ColumnSet\",\n+                        \"separator\": True,\n+                        \"columns\": [\n+                            {\n+                            \"type\": \"Column\",\n+                            \"width\": \"stretch\",\n+                            \"items\": []\n+                            }\n+                        ]\n+                    },\n+                    # {\n+                    #     \"type\": \"TextBlock\",\n+                    #     \"text\": \"Test Execution Summary\",\n+                    #     \"weight\": \"Bolder\",\n+                    #     \"size\": \"Medium\",\n+                    #     \"horizontalAlignment\": \"Left\",\n+                    #     \"spacing\": \"Medium\"\n+                    # },\n+                    self.build_summary_columnset(msg)\n+                ]\n+            }\n+        }\n+\n+        # send message\n+        if not webex_id:\n+            logger.warning(\"No Webex ID provided for notification.\")\n+            return\n+        logger.info(f\"Sending Webex message to {webex_id}\")\n+        # check if webex_id is a valid email format\n+        if not isinstance(webex_id, str):\n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+            return\n+        if webex_id.count('@')==1:\n+            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n+            api.messages.create(\n+                toPersonEmail=webex_id,\n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        elif webex_id.count('-')==4:\n+            # api.messages.create(\n+            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n+            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n+            # )\n+            api.messages.create(\n+                roomId=webex_id, \n+                text=subject,\n+                attachments=[card_content]\n+            )\n+        else:\n+            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n+\n+if __name__ == \"__main__\":\n+    # Example usage\n+    nm = NotificationManager()\n+    nm.notify_user_by_webex(\n+        subject=\"Regression Notification\",\n+        content=sys.argv[2:],\n+        webex_id=sys.argv[1].strip()\n+    )\n"
                },
                {
                    "date": 1769231478300,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -161,9 +161,9 @@\n         api = WebexTeamsAPI(\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n \n-        # get room list for debug\n+        # get room list of webex bot\n         # rooms = api.rooms.list()\n         # for room in rooms:\n         #     print(room.title, room.id)\n \n@@ -251,258 +251,4 @@\n         subject=\"Regression Notification\",\n         content=sys.argv[2:],\n         webex_id=sys.argv[1].strip()\n     )\n-\"\"\"\n-Notification manager for the Lab Management System.\n-\"\"\"\n-\n-import logging\n-from typing import Dict, Any\n-import smtplib\n-from webexteamssdk import WebexTeamsAPI\n-from email.mime.text import MIMEText\n-from email.mime.multipart import MIMEMultipart\n-import sys\n-\n-# Configure logging\n-logger = logging.getLogger(__name__)\n-\n-\n-class NotificationManager:\n-    \"\"\"\n-    Manages notifications for the Lab Management System.\n-    Handles Webex notifications.\n-    \"\"\"\n-\n-    def __init__(self):\n-        \"\"\"Initialize the notification manager.\"\"\"\n-        logger.info(\"Notification Manager initialized\")\n-\n-    def send_device_notification(\n-        self, device_id: int, message: str, data: Dict[str, Any]\n-    ):\n-        \"\"\"\n-        Send a notification about a device event.\n-\n-        Args:\n-            device_id: ID of the device\n-            message: Notification message\n-            data: Additional data for the notification\n-        \"\"\"\n-        # This is a placeholder implementation\n-        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n-\n-        # TODO: Implement actual notification sending via Webex\n-        # For now, just log the message\n-        return True\n-\n-    def notify_user_by_email(self, subject, context, email_list):\n-        # subject = f'Subject: {subject} \\n\\n {context}'\n-        # configure SMTP server\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        sender_email = \"no-reply@cisco.com\"\n-\n-        recipient_email = email_list\n-        message = MIMEMultipart()\n-        message[\"Subject\"] = subject\n-        message[\"From\"] = sender_email\n-        message[\"To\"] = \", \".join(email_list)\n-        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n-        try:\n-            with smtplib.SMTP(smtp_server, port) as server:\n-                server.starttls()\n-                server.sendmail(sender_email, recipient_email, message.as_string())\n-\n-                logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-\n-    def notify_user_by_email_with_plain_txt(\n-        self, subject: str, body: str, email_list: list\n-    ):\n-        msg = MIMEMultipart()\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n-        msg = MIMEMultipart(\"alternative\")\n-        msg[\"From\"] = \"no-reply@cisco.com\"\n-        msg[\"To\"] = \", \".join(email_list)\n-        msg[\"Subject\"] = subject\n-        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n-\n-        smtp_server = \"outbound.cisco.com\"\n-        port = 25\n-        recipient_email = email_list\n-        try:\n-            server = smtplib.SMTP(smtp_server, port)\n-            server.starttls()\n-            text = msg.as_string()\n-            server.sendmail(msg[\"From\"], recipient_email, text)\n-            logger.info(\"send mail successfully\")\n-        except Exception as e:\n-            logger.warning(f\"fail to send mail: {e}\")\n-        finally:\n-            server.quit()\n-\n-    def build_summary_columnset(self, summary_text):\n-        \"\"\"\n-        Convert 'key : value' lines into an Adaptive Card ColumnSet\n-        compatible with Cisco Webex (Adaptive Card v1.2).\n-        \"\"\"\n-        left_items = []\n-        right_items = []\n-\n-        for line in summary_text.strip().splitlines():\n-            if \":\" not in line:\n-                continue\n-\n-            key, value = map(str.strip, line.split(\":\", 1))\n-\n-            if value == \"\" or key == \"\":\n-                continue\n-            if key.count(\"percentage\"):\n-                key = key.replace(\"percentage\", \"%\")\n-\n-            left_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": key,\n-                \"wrap\": True\n-            })\n-\n-            right_items.append({\n-                \"type\": \"TextBlock\",\n-                \"text\": value,\n-                \"wrap\": True\n-            })\n-\n-        return {\n-            \"type\": \"ColumnSet\",\n-            \"columns\": [\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": left_items\n-                },\n-                {\n-                    \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n-                    \"horizontalAlignment\": \"Left\",\n-                    \"items\": right_items\n-                }\n-            ]\n-        }\n-\n-    def notify_user_by_webex(self, subject, content, webex_id):\n-        api = WebexTeamsAPI(\n-            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n-        )\n-\n-        # get room list for debug\n-        # rooms = api.rooms.list()\n-        # for room in rooms:\n-        #     print(room.title, room.id)\n-\n-        if content.count('Test results summary:'):\n-            subject = \"Test Results Summary\"\n-        content_lst = \" \".join(content).split('|')\n-        if content_lst[0].count(\"db_build_id\"):\n-            db_build_id = content_lst[0].split(':')[1].strip()\n-            subject = f\"{subject} [{db_build_id}](http://vip-oldregressdb.cisco.com:8080/regressdb/{db_build_id}/)\"\n-            msg = \"\\n\".join(content_lst[1:])\n-        else:\n-            msg = \"\\n\".join(content_lst)\n-\n-        card_content = {\n-            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n-            \"content\": {\n-                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n-                \"type\": \"AdaptiveCard\",\n-                \"version\": \"1.3\",\n-                \"body\": [\n-                    {\n-                        \"type\": \"TextBlock\",\n-                        \"text\": subject,\n-                        \"weight\": \"Bolder\",\n-                        \"horizontalAlignment\": \"Center\",\n-                        \"size\": \"Medium\"\n-                    },\n-                    {\n-                        \"type\": \"ColumnSet\",\n-                        \"separator\": True,\n-                        \"columns\": [\n-                            {\n-                            \"type\": \"Column\",\n-                            \"width\": \"stretch\",\n-                            \"items\": []\n-                            }\n-                        ]\n-                    },\n-                    # {\n-                    #     \"type\": \"TextBlock\",\n-                    #     \"text\": \"Test Execution Summary\",\n-                    #     \"weight\": \"Bolder\",\n-                    #     \"size\": \"Medium\",\n-                    #     \"horizontalAlignment\": \"Left\",\n-                    #     \"spacing\": \"Medium\"\n-                    # },\n-                    self.build_summary_columnset(msg)\n-                ]\n-            }\n-        }\n-\n-        # send message\n-        if not webex_id:\n-            logger.warning(\"No Webex ID provided for notification.\")\n-            return\n-        logger.info(f\"Sending Webex message to {webex_id}\")\n-        # check if webex_id is a valid email format\n-        if not isinstance(webex_id, str):\n-            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-            return\n-        if webex_id.count('@')==1:\n-            #api.messages.create(toPersonEmail=webex_id, text=contextStr)\n-            api.messages.create(\n-                toPersonEmail=webex_id,\n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        elif webex_id.count('-')==4:\n-            # api.messages.create(\n-            #     # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n-            #     roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n-            # )\n-            api.messages.create(\n-                roomId=webex_id, \n-                text=subject,\n-                attachments=[card_content]\n-            )\n-        else:\n-            logger.warning(f\"Invalid Webex ID format: {webex_id}\")\n-\n-if __name__ == \"__main__\":\n-    # Example usage\n-    nm = NotificationManager()\n-    nm.notify_user_by_webex(\n-        subject=\"Regression Notification\",\n-        content=sys.argv[2:],\n-        webex_id=sys.argv[1].strip()\n-    )\n"
                },
                {
                    "date": 1769232500604,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -162,11 +162,11 @@\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n \n         # get room list of webex bot\n-        # rooms = api.rooms.list()\n-        # for room in rooms:\n-        #     print(room.title, room.id)\n+        rooms = api.rooms.list()\n+        for room in rooms:\n+            print(f\"{room.title}: {room.id}\")\n \n         if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n"
                },
                {
                    "date": 1769232518217,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -162,11 +162,11 @@\n             access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n         )\n \n         # get room list of webex bot\n-        rooms = api.rooms.list()\n-        for room in rooms:\n-            print(f\"{room.title}: {room.id}\")\n+        # rooms = api.rooms.list()\n+        # for room in rooms:\n+        #     print(f\"{room.title}: {room.id}\")\n \n         if content.count('Test results summary:'):\n             subject = \"Test Results Summary\"\n         content_lst = \" \".join(content).split('|')\n"
                },
                {
                    "date": 1770464335542,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -143,9 +143,9 @@\n             \"type\": \"ColumnSet\",\n             \"columns\": [\n                 {\n                     \"type\": \"Column\",\n-                    \"width\": \"stretch\",\n+                    \"width\": \"auto\",\n                     \"horizontalAlignment\": \"Left\",\n                     \"items\": left_items\n                 },\n                 {\n"
                }
            ],
            "date": 1768696621212,
            "name": "Commit-0",
            "content": "\"\"\"\nNotification manager for the Lab Management System.\n\"\"\"\n\nimport logging\nfrom typing import Dict, Any\nimport smtplib\nfrom webexteamssdk import WebexTeamsAPI\nfrom email.mime.text import MIMEText\nfrom email.mime.multipart import MIMEMultipart\nimport sys\n\n# Configure logging\nlogger = logging.getLogger(__name__)\n\n\nclass NotificationManager:\n    \"\"\"\n    Manages notifications for the Lab Management System.\n    Handles Webex notifications.\n    \"\"\"\n\n    def __init__(self):\n        \"\"\"Initialize the notification manager.\"\"\"\n        logger.info(\"Notification Manager initialized\")\n\n    def send_device_notification(\n        self, device_id: int, message: str, data: Dict[str, Any]\n    ):\n        \"\"\"\n        Send a notification about a device event.\n\n        Args:\n            device_id: ID of the device\n            message: Notification message\n            data: Additional data for the notification\n        \"\"\"\n        # This is a placeholder implementation\n        logger.info(f\"NOTIFICATION: Device {device_id}: {message}\")\n\n        # TODO: Implement actual notification sending via Webex\n        # For now, just log the message\n        return True\n\n    def notify_user_by_email(self, subject, context, email_list):\n        # subject = f'Subject: {subject} \\n\\n {context}'\n        # configure SMTP server\n        smtp_server = \"outbound.cisco.com\"\n        port = 25\n        sender_email = \"no-reply@cisco.com\"\n\n        recipient_email = email_list\n        message = MIMEMultipart()\n        message[\"Subject\"] = subject\n        message[\"From\"] = sender_email\n        message[\"To\"] = \", \".join(email_list)\n        message.attach(MIMEText(context, \"plain\", \"utf-8\"))\n        try:\n            with smtplib.SMTP(smtp_server, port) as server:\n                server.starttls()\n                server.sendmail(sender_email, recipient_email, message.as_string())\n\n                logger.info(\"send mail successfully\")\n        except Exception as e:\n            logger.warning(f\"fail to send mail: {e}\")\n\n    def notify_user_by_email_with_plain_txt(\n        self, subject: str, body: str, email_list: list\n    ):\n        msg = MIMEMultipart()\n        msg[\"From\"] = \"no-reply@cisco.com\"\n        msg[\"To\"] = \", \".join(email_list)\n        msg[\"Subject\"] = subject\n        msg.attach(MIMEText(body, \"plain\", \"utf-8\"))\n\n        smtp_server = \"outbound.cisco.com\"\n        port = 25\n        recipient_email = email_list\n        try:\n            server = smtplib.SMTP(smtp_server, port)\n            server.starttls()\n            text = msg.as_string()\n            server.sendmail(msg[\"From\"], recipient_email, text)\n            logger.info(\"send mail successfully\")\n        except Exception as e:\n            logger.warning(f\"fail to send mail: {e}\")\n        finally:\n            server.quit()\n\n    def notify_user_by_email_with_html(self, subject: str, body: str, email_list: list):\n        msg = MIMEMultipart(\"alternative\")\n        msg[\"From\"] = \"no-reply@cisco.com\"\n        msg[\"To\"] = \", \".join(email_list)\n        msg[\"Subject\"] = subject\n        msg.attach(MIMEText(body, \"html\", \"utf-8\"))\n\n        smtp_server = \"outbound.cisco.com\"\n        port = 25\n        recipient_email = email_list\n        try:\n            server = smtplib.SMTP(smtp_server, port)\n            server.starttls()\n            text = msg.as_string()\n            server.sendmail(msg[\"From\"], recipient_email, text)\n            logger.info(\"send mail successfully\")\n        except Exception as e:\n            logger.warning(f\"fail to send mail: {e}\")\n        finally:\n            server.quit()\n\n    def notify_user_by_webex(self, subject, context, to_list):\n        # 初始化 API\n        api = WebexTeamsAPI(\n            access_token=\"ZmNmM2M2MzktNTEzMy00N2VhLTgzOWMtNDE0N2E3NmRjYmE5Y2I5OTVmNDgtMGE5_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f\"\n        )\n        summary = \" \".join(context).split('|')\n        contextStr = subject + \"\\n\" + \"\\n\".join(summary)\n\n        card_content = {\n            \"contentType\": \"application/vnd.microsoft.card.adaptive\",\n            \"content\": {\n                \"$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",\n                \"type\": \"AdaptiveCard\",\n                \"version\": \"1.2\",\n                \"body\": [\n                    {\n                        \"type\": \"TextBlock\",\n                        \"text\": \"Notification Card\",\n                        \"weight\": \"Bolder\",\n                        \"size\": \"Medium\"\n                    },\n                    {\n                        \"type\": \"TextBlock\",\n                        \"text\": \"This card contains important information.\",\n                        \"wrap\": True\n                    }\n                ]\n            }\n        }\n\n        # 发送消息\n        for person in to_list:\n            if not person:\n                continue\n            logger.info(f\"Sending Webex message to {person}\")\n            # check if person is a valid email format\n            if not isinstance(person, str):\n                logger.warning(f\"Invalid email format: {person}\")\n                continue\n            if person.count('@'):\n                #api.messages.create(toPersonEmail=person, text=contextStr)\n                api.cards.create(\n                    toPersonEmail=person,\n                    markdown=\"**This is a card**\",\n                    title=\"Notification Card\",\n                    text=\"This card contains important information.\",\n                )\n            else:\n                api.messages.create(\n                    # roomId=\"c582edc0-3443-11ef-a59e-497ab6833a4a\", text=contextStr\n                    roomId=\"306d3220-eadd-11f0-8ed5-e7669d71f2e3\", text=contextStr\n                )\n\n\nif __name__ == \"__main__\":\n    # Example usage\n    nm = NotificationManager()\n    nm.notify_user_by_webex(\n        subject=\"Regression Notification\",\n        context=sys.argv[2:],\n        to_list=[sys.argv[1]],\n    )\n"
        }
    ]
}